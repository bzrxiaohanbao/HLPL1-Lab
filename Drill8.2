#include <iostream>

using namespace std;
void swap_v(int a,int b){ int temp; temp = a, a=b; b=temp; }//It can be compiled and passed,x=7,y=9
//void swap_r(int &a,int &b){ int temp; temp = a, a=b; b=temp; }//It can't be compiled. The input parameter needs to point to a variable, while 7 and 9 in the program are long variables
//void swap_cr(const int& a,const int& b){ int temp; temp = a, a=b; b=temp; }//The function cannot be compiled because a and B cannot be changed
int main()
{


int x= 7;

int y =9;

swap_v(x,y);
swap_v(7,9);
const int cx= 7;
const int cy= 9;
swap_v(cx,cy);
swap_v(7.7,9.9);
double dx = 7.7;
double dy= 9.9;
swap_v(dx,dy);
swap_v(7.7,9.9);

cout<<x<<' '<<y<<endl;
}
